#include<stdio.h>
#include<stdlib.h>

int ispalindrome(int n);

int ispalindrome()
{
	int i = 0;
	int* arr1 = (int*)malloc(sizeof(int));
	int num = n, d;
	int temp;
	while(num)
	{
		d = num%10;
		num = num/10;

	}
}

int main()
{
	for(int i = 900; i < 1000; i++)
		for(int j = i; j < 1000; j++)
}